/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
import React from "react";

const Title = ({ title }) => {
  return <h2 className="font-dm text-[39px] font-bold capitalize text-[#262626]">{title}</h2>;
};

export default Title;
